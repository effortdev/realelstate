name: Java CI with Gradle and Deploy to Azure

on:
  push:
    branches: [ "master" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build with Gradle
        run: |
          chmod +x gradlew
          ./gradlew bootJar

      - name: Docker build & push
        run: |
          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
          docker build -t ktjone/realestate-app .
          docker push ktjone/realestate-app


      - name: Deploy to Azure
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.AZURE_HOST }}
          username: azureuser  # 직접 적거나 secrets.AZURE_USER 사용
          key: ${{ secrets.AZURE_SSH_KEY }} # 패스워드 대신 이 줄을 넣으세요!
          script: |
            cd ~/realestate
            # 1. 기존 컨테이너 중지 및 삭제 (안전한 재시작을 위해)
            docker-compose down
            # 2. 최신 이미지 가져오기
            docker-compose pull
            # 3. 백그라운드 실행
            docker-compose up -d